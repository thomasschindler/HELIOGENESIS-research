this thing attempts to read PDF files from INPUT directory, create a RAG for each and run some queries on openai via langchain. this is completely incomplete. 

How to run it.

1) copy env to .env
2) populate with your openai key and add the path to this repo 
3) add some pdf files to input 
4) run it: docker-compose up